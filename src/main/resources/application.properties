# Configuración del servidor
server.port=8080

# Configuración de PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/Haidainversiones.Llantas
spring.datasource.username=Haidainversiones. Llantas_user
spring.datasource.password=llantas123
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Nombre de la aplicación
spring.application.name=haidainversiones-llantas

# JWT Configuration
jwt.secret=MiClaveSecretaSuperSeguraParaJWT2026HaidaInversionesLlantasBackendSecureKey12345
jwt.expiration=86400000

# CORS
spring.web.cors.allowed-origins=http://localhost:4200
